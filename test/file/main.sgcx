/*
Opening files in SGC++.

This looks really complicated.
    - yes it's pretty complicated. SGC++ doesn't have a built in file reading system (keyword wise).
        We WILL be working on this soon.
        
Squirrel Gay Acorn

*/

import os


var fd = os.open("greg.txt", os.O_RDONLY)

var content = os.read(fd, 300)
var greg = content.decode('utf-8')
gPrintln(greg)


os.close(fd)
